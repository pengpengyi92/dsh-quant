# v0.16.0 更新记录（2026-08-16）

**域驱动目录重构（PDAT→PET 映射）+ 多交易所 fallback**

- src/ 重组 5 域（反对脚本式平铺）：
  - dsh-data（PDAT 映射）：market/data-guide/stats/resample
  - dsh-alpha（PAAT 映射）：indicators/factor
  - dsh-ml（PCPT 映射）：backtest/metrics
  - dsh-risk（PRT 映射）：risk（含 Kupiec）
  - dsh-execution（PET 映射）：chart/fund/report（不接实盘声明）
- 每域 README：映射说明 + 模块清单 + 原则 + 边界
- 包级 API 不变（34 工具 + re-export 向后兼容）；100 单测全过
- 实战事件：Binance 451 地区封锁 + Bybit CloudFront → verify 加 fallback 链
  （binance → okx → bybit）自动切换——多交易所设计的 resilience 价值实证
